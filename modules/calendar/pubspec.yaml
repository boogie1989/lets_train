name: calendar
description: "A new Flutter package project."
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  ui_kit:

  flutter_hooks:
  riverpod:
  flutter_riverpod:
  hooks_riverpod:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: