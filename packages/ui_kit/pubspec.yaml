name: ui_kit
description: "A new Flutter package project."

version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks:
  hooks_riverpod:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  shaders:
    - shaders/screen_shader_background.frag
    - shaders/box_shadow.frag

